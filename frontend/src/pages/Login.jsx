import { useState } from 'react'; import { useNavigate, Link } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function Login(){const [form,setForm]=useState({email:'',password:''}); const [err,setErr]=useState(''); const {login}=useAuth(); const nav=useNavigate(); async function submit(e){e.preventDefault(); try{const u=await login(form); nav(u.role==='admin'?'/admin':'/student')}catch(x){setErr(x.response?.data?.message||'Login failed')}} return <div className="mx-auto grid min-h-[80vh] max-w-md place-items-center p-6"><form onSubmit={submit} className="card w-full"><h1 className="mb-6 text-3xl font-black">Login</h1>{err&&<p className="mb-3 text-red-300">{err}</p>}<input className="input mb-3" placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})}/><input className="input mb-5" type="password" placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn w-full">Login</button><p className="mt-4 text-sm text-slate-400">No account? <Link className="text-cyan-400" to="/register">Register</Link></p></form></div>}
