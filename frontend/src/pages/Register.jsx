import { useState } from 'react'; import { useNavigate, Link } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:'',role:'student',matricNo:''}); const [err,setErr]=useState(''); const {register}=useAuth(); const nav=useNavigate(); async function submit(e){e.preventDefault(); try{const u=await register(form); nav(u.role==='admin'?'/admin':'/student')}catch(x){setErr(x.response?.data?.message||'Registration failed')}} return <div className="mx-auto grid min-h-[80vh] max-w-md place-items-center p-6"><form onSubmit={submit} className="card w-full"><h1 className="mb-6 text-3xl font-black">Create Account</h1>{err&&<p className="mb-3 text-red-300">{err}</p>}<input className="input mb-3" placeholder="Full name" onChange={e=>setForm({...form,name:e.target.value})}/><input className="input mb-3" placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})}/><input className="input mb-3" placeholder="Matric number optional" onChange={e=>setForm({...form,matricNo:e.target.value})}/><select className="input mb-3" onChange={e=>setForm({...form,role:e.target.value})}><option value="student">Student</option><option value="admin">Admin</option></select><input className="input mb-5" type="password" placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn w-full">Register</button><p className="mt-4 text-sm text-slate-400">Already registered? <Link className="text-cyan-400" to="/login">Login</Link></p></form></div>}
